{
  "name": "clean-wallet-package",
  "version": "1.0.2",
  "scripts": {
    "build": "react-app-rewired build",
    "lint": "eslint src server",
    "dev": "react-app-rewired start"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
